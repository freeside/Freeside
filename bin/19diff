#!/usr/bin/perl

my $file = shift;

chomp(my $dir = `pwd`);
$dir =~ s/freeside\//freeside1.9\//;

#$cmd = "diff -u $file $dir/$file";
$cmd = "diff -u $dir/$file $file";
print "$cmd\n";
system($cmd);

